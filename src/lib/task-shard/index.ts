export { initBoard } from './board';
export { loadTasks, saveTasks, addTask, updateTask, deleteTask, generateId } from './storage';
export { COLUMNS } from './types';
export type { Task, ColumnId } from './types';
